const initialState = {
    user: {},
    is_login: false,
    is_admin: false
}

export const userReducer = (state = initialState, action) => {
    switch (action.type) {
        default: {
            return state
        }
    }
}